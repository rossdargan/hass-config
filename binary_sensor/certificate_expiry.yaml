  - platform: template
    sensors:
      certificate_nearly_expired:
        value_template: '{{ int(states.sensor.ssl_certificate_expiry.state) < 15 }}'
        friendly_name: 'Certificate has nearly expired'
