  - alias: Monzo Balance
    initial_state: 'on'
    trigger:
      - platform: numeric_state
        entity_id: sensor.ross_monzo
        below: 20
    action:
      - service: notify.slackbot
        data:
          message: "Low on Monzo funds!"   
