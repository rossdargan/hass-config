  - platform: hue

# Extract the temperature from the motion sensor
  - platform: template
    sensors:
      hall_temperature:
        friendly_name: 'Hall temperature'
        value_template: '{{states.sensor.hall_motion_motion.attributes.temperature}}'
        unit_of_measurement: °C
