  - platform: template
    sensors:
      office_door_unlocked:
        entity_id: lock.office_door
        value_template: >-
          {% set users= 
          [ 'Ross: Keys', 
          'Ross: Wallet',
          'Kath: Keys',
          'Shared Card',
          'Kath: Card' ] %}
          {% set unlockStatus = state_attr('lock.office_door', 'lock_status') %}
          {% if "Unlocked by" in unlockStatus  %}
          {% if "Unlocked by NFC Tag or Card by user " in unlockStatus  %}
          {% set user = unlockStatus | replace("Unlocked by NFC Tag or Card by user ", "") |int %}
          {{ users[user-1] }}
          {% elif 'Unlocked by Bluetooth' in unlockStatus  %}
          {{ 'Phone' }}
          {% endif %}          
          {% endif %}          
#kim, dad, kath card
      front_door_unlocked:
        entity_id: lock.front_door
        value_template: >-
          {% set users= 
          [ 'Ross: Keys', 
          '2',
          '3',
          'Kath: Keys',
          '5',
          'Ross: Wallet',
          'Shared Card' ] %}
          {% set unlockStatus = state_attr('lock.front_door', 'lock_status') %}
          {% if "Unlocked by" in unlockStatus  %}
          {% if "Unlocked by NFC Tag or Card by user " in unlockStatus  %}
          {% set user = unlockStatus | replace("Unlocked by NFC Tag or Card by user ", "") |int %}
          {{ users[user-1] }}
          {% elif 'Unlocked by Bluetooth' in unlockStatus  %}
          {{ 'Phone' }}
          {% endif %}          
          {% endif %}          

      playroom_door_unlocked:
        entity_id: lock.playroom_door
        value_template: >-
          {% set users= 
          [ 'Ross: Keys', 
          'Ross: Wallet',
          'Kath: Keys',
          'Shared Card',
          'Adam: Epi Pen'] %}
          {% set unlockStatus = state_attr('lock.playroom_door', 'lock_status') %}
          {% if "Unlocked by" in unlockStatus  %}
          {% if "Unlocked by NFC Tag or Card by user " in unlockStatus  %}
          {% set user = unlockStatus | replace("Unlocked by NFC Tag or Card by user ", "") |int %}
          {{ users[user-1] }}
          {% elif 'Unlocked by Bluetooth' in unlockStatus  %}
          {{ 'Phone' }}
          {% endif %}          
          {% endif %}          
                    