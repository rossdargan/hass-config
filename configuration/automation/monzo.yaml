  - alias: Monzo Balance
    initial_state: 'on'
    trigger:
      - platform: numeric_state
        entity_id: sensor.ross_monzo
        below: 150
    action:
      - service: notify.html5
        data:
          message: "You have less than £150 in monzo!"   
      - service: calendar.todoist_new_task
        data:
          content: 'Top up monzo'
          project: 'Personal'
          priority: 1
          due_date_string: 'Today'
