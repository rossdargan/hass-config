tv_source_xbox:
   alias: 'Turn on the xbox'
   sequence:
    - service: media_player.select_source
      data_template:
        entity_id: media_player.yamaha_reciever
        source: "HDMI4"

tv_source_switch:
   alias: 'Turn on the switch'
   sequence:
    - service: media_player.select_source
      data_template:
        entity_id: media_player.yamaha_reciever
        source: "HDMI1"

tv_source_shield:
   alias: 'Turn on the Shield'
   sequence:
    - service: media_player.select_source
      data_template:
        entity_id: media_player.yamaha_reciever
        source: "HDMI6"

tv_source_spotify:
   alias: 'Turn on spotify'
   sequence:
    - service: media_player.select_source
      data_template:
        entity_id: media_player.yamaha_reciever
        source: "SPOTIFY"
